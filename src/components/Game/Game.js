import React from 'react'
import './Game.css'

const Game = () => {
  // props:
  // - all of the users selections

  // state
  // - array of questions
  // - current question
  // - num questions correct

  // useEffect dependent on user selections
  // - assemble the url
  // - fetch to the API
  // - save all questions in state 

  // correctQuestions passed down to Questions as props

  // return
  // - conditionally render current question
  // - map over all questions and only render current one
  // - next question button
  //   - onClick that increments current question
  // when current question = number of questions requested
  // - display Dashboard component, pass it num questions correct
  return (
    <div>Game</div>
  )
}

export default Game